# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://help.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
# Enable version updates for npm
- package-ecosystem: "npm"
  # Look for `package.json` and `lock` files in the `root` directory
  directory: "/src/test/data_table_component/data_table_test/frontend"
  # Check the npm registry for updates every day (weekdays)
  schedule:
    interval: "daily"# Enable version updates for npm
- package-ecosystem: "npm"
  # Look for `package.json` and `lock` files in the `root` directory
  directory: "/src/lib/data_manager/data_table_component/data_table/frontend"
  # Check the npm registry for updates every day (weekdays)
  schedule:
    interval: "daily"# Enable version updates for npm
- package-ecosystem: "npm"
  # Look for `package.json` and `lock` files in the `root` directory
  directory: "/src/lib/data_editor/streamlit_labelstudio/frontend"
  # Check the npm registry for updates every day (weekdays)
  schedule:
    interval: "daily"

# - package-ecosystem: "pip"
#   # Look for `requirements.txt` files in the `root` directory
#   directory: "/"
#   # Check the npm registry for updates every day (weekdays)
#   schedule:
#     interval: "daily"
