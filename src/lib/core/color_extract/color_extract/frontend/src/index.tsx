import React from "react"
import ReactDOM from "react-dom"
import ColorExract from "./ColorExtract"

ReactDOM.render(
  <React.StrictMode>
    <ColorExract />
  </React.StrictMode>,
  document.getElementById("root")
)
